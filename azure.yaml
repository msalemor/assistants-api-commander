# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: assistants-api-playground
metadata:
  template: assistants-api-playground@0.0.1
services:
  backend:
    language: python
    project: ./src/backend
    host: containerapp
  frontend:
    language: js
    project: ./src/frontend
    host: containerapp